# Submissions

제출 CSV는 이 폴더에 둘 수 있지만 `.gitignore`에 의해 Git에는 올라가지 않습니다.

현재 V1 표준 파일명은 `submit_v1_weighted_quantile_extratrees.csv`입니다.

제출 후 `docs/leaderboard.md`에 제출번호, 파일명, 시각, Public Score, Private Score, 실험 번호를 기록합니다.
